const UNIT_PX = 5

export default {
  xsmall: `${Math.floor(UNIT_PX / 2)}px`,
  zero: `${UNIT_PX * 0}px`,
  small: `${UNIT_PX * 1}px`,
  medium: `${UNIT_PX * 2}px`,
  large: `${UNIT_PX * 4}px`,
  xlarge: `${UNIT_PX * 8}px`,
  xxlarge: `${UNIT_PX * 12}px`
}